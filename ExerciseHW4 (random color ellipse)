
function setup() { 
  
  createCanvas(400, 400);
 

  background(0);
  
colorMode (HSB);
  var x = 20;
  var y = 20;
  var S = 100;
  var B = 100;
	var H = 0;
	
  for (i = 20; i<=380; i += 20)
  {
    for (j = 20; j<=380; j+= 20)
    {
	noStroke();
  
  ellipse (j,i,15,15);
  fill(H,50,100,255);
  var H = random(360);
    }
  }
}
function draw() { 

}
