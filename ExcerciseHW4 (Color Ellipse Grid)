
function setup() { 
  
  createCanvas(400, 400);
 

  background(255);
  
colorMode (HSB);
	var H = 0;
	
  for (i = 20; i<=380; i += 20)
  {
    for (j = 20; j<=380; j+= 20)
    {
	noStroke();
  
  fill(H,100,100);
  ellipse (j,i,15,15);

  H = H + 0.6;
    }
  } 
}
