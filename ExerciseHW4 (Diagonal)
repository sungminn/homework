function setup() { 
  createCanvas(400, 400);
} 


function draw() { 
  background(0);

  var x = 20;
  var y = 20;
  
  for (var x = 20; x<width; x=x+20) {
	colorMode (HSB); 
	fill(255); 
  ellipse (x,x,20,20);
  }
  
}
